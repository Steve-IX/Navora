{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd shared && npm install && npm run build && cd ../backend && npm install && npm run build && ls -la dist/ | head -20"
  },
  "deploy": {
    "startCommand": "cd backend && ls -la dist/ && node dist/main",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

